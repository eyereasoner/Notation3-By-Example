@prefix : <urn:example:> .
@prefix log: <http://www.w3.org/2000/10/swap/log#> .

# ∀x : ~( Beetle a Car )
(?x) log:onNegativeSurface {
    :Beetle a :Car .
} .

(?S ?O) log:onQuestionSurface {
    # ∀S,O : ~(S a O)
    () log:onNegativeSurface {
        ?S a ?O .
    } .
    () log:onAnswerSurface {
        :test :is true .
    }
} .
